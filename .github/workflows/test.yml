# name: CI

# permissions: {}

# on:
#   push:
#   pull_request:
#   workflow_dispatch:

# jobs:
#   check:
#     name: Foundry project
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#     steps:
#       - uses: actions/checkout@v6
#         with:
#           persist-credentials: false
#           submodules: recursive

#       - name: Install Foundry
#         uses: foundry-rs/foundry-toolchain@v1

#       - name: Show Forge version
#         run: forge --version

#       - name: Run Forge fmt
#         run: forge fmt --check

#       - name: Run Forge build
#         run: forge build --sizes

#       - name: Run Forge tests
#         run: forge test -vvv

name: Web3 Security Pipeline

permissions:
  contents: read

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  check:
    name: Foundry Project Security Scan
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Slither
        run: |
          pip3 install slither-analyzer
          npm install -g @solidity-parser/parser

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1

      - name: Show Forge version
        run: forge --version

      - name: Run Forge fmt
        run: forge fmt --check

      - name: Run Forge build
        run: forge build --sizes

      - name: Run Forge tests
        run: forge test -vvv

      - name: Run Slither Security Scan
        run: slither . --fail-medium
